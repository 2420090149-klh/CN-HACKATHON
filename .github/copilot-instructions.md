# Workspace Instructions

- Keep the phishing detector focused on URL risk analysis and avoid adding unrelated features.
- Prefer changes that improve the detector's signal quality, readability, and test coverage.
- Keep the Streamlit UI simple and trustworthy: clear status, clear signals, and minimal clutter.
- When adding new detection logic, cover it with tests in `tests/test_analyzer.py`.
